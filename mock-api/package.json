{
  "name": "json-server",
  "version": "1.0.0",
  "description": "Mock API with json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch data/db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
 